/* Global Reset and Variables */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
}

:root {
    --bg-color: #121212;
    --card-bg: #1e1e1e;
    --text-color: #e0e0e0;
    --text-muted: #a0a0a0;
    --accent-color: #ff4a4a; /* Vibrant Red accent */
    --border-color: #2d2d2d;
}

body {
    background-color: var(--bg-color);
    color: var(--text-color);
    line-height: 1.6;
}

.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Navigation Header */
header {
    background-color: rgba(18, 18, 18, 0.95);
    position: sticky;
    top: 0;
    z-index: 100;
    border-bottom: 1px solid var(--border-color);
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1100px;
    margin: 0 auto;
    padding: 20px;
}

.logo {
    font-weight: 700;
    font-size: 1.2rem;
    color: #fff;
    text-decoration: none;
    letter-spacing: 0.5px;
}

nav ul {
    display: flex;
    list-style: none;
}

nav ul li {
    margin-left: 20px;
}

nav ul li a {
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.2s ease;
}

nav ul li a:hover {
    color: #fff;
}

/* Hero Section */
.hero-section {
    padding: 80px 0;
}

.profile-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 40px;
    align-items: center;
}

.profile-text h1 {
    font-size: 2.5rem;
    margin-bottom: 20px;
    color: #fff;
}

.profile-text p {
    color: var(--text-muted);
    margin-bottom: 15px;
    font-size: 1.1rem;
}

.social-links a {
    display: inline-block;
    margin-right: 15px;
    margin-top: 15px;
    color: #fff;
    text-decoration: none;
    background: var(--card-bg);
    padding: 8px 16px;
    border-radius: 4px;
    border: 1px solid var(--border-color);
    transition: all 0.2s ease;
}

.social-links a:hover {
    background: var(--accent-color);
    border-color: var(--accent-color);
}

.profile-img-placeholder {
    background-color: var(--card-bg);
    border: 1px dashed var(--border-color);
    height: 300px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
}

/* Layout Dividers */
.section-divider {
    border: 0;
    height: 1px;
    background: var(--border-color);
    max-width: 1100px;
    margin: 0 auto;
}

/* Projects Section */
.projects-section {
    padding: 60px 0;
}

.projects-section h2 {
    font-size: 2rem;
    margin-bottom: 40px;
    color: #fff;
}

.projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
}

.project-card {
    background-color: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    overflow: hidden;
    transition: transform 0.2s ease, border-color 0.2s ease;
}

.project-card:hover {
    transform: translateY(-4px);
    border-color: #444;
}

.card-img {
    height: 200px;
    background-color: #2a2a2a;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
    font-size: 0.9rem;
}

.card-content {
    padding: 20px;
}

.card-content h3 {
    margin-bottom: 10px;
    color: #fff;
}

.card-content p {
    color: var(--text-muted);
    font-size: 0.95rem;
    margin-bottom: 20px;
    height: 70px; /* Limits size to match grids cleanly */
    overflow: hidden;
}

.read-more {
    color: var(--accent-color);
    text-decoration: none;
    font-weight: 500;
}

.read-more:hover {
    text-decoration: underline;
}

/* Footer styling */
footer {
    text-align: center;
    padding: 40px 20px;
    border-top: 1px solid var(--border-color);
    color: var(--text-muted);
    font-size: 0.9rem;
}

footer a {
    color: var(--text-color);
    text-decoration: none;
}

/* Responsive adjustments */
@media(max-width: 768px) {
    .profile-grid {
        grid-template-columns: 1fr;
    }
    .profile-img-placeholder {
        order: -1; /* Puts image on top on mobile layouts */
        height: 250px;
    }
}